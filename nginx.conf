server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }

    location /promo {
        try_files $uri $uri/ /;
    }

    # All paths starting with /legal will try the file, then fallback to viewer.html
    location /legal {
        try_files $uri $uri/ /legal/viewer.html;
    }

    error_page 404 /;
}
