services:
  web:
    image: nginx:alpine
    container_name: arlist-main-site
    restart: always
    volumes:
      - ./html:/usr/share/nginx/html
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    ports:
      - "8086:80"
